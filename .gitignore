# Downloaded and derived datasets — never committed.
# See docs/DATA_SOURCES.md: Polaris (CC BY-SA 4.0) must not be redistributed via this repo.
data/raw/
data/processed/

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Environment
.env
.env.local

# macOS
.DS_Store

# Pairwise model artifacts. Fitted on the CC BY-SA Polaris corpus, so the artifact is
# kept reproducible rather than distributed — `scripts/train_pairwise.py` rebuilds it.
# Sanitised metrics (no Polaris identifiers) are committed under data/evals/intake/.
data/models/

# Exported operator correlation labels. Generated from whatever reviews the local
# database holds, so the file is reproducible rather than committed —
# `scripts/export_correlation_labels.py` rebuilds it. Committing it would also publish
# runtime ticket text.
data/evals/labels/
.vercel
.env*

# Installed by the Vercel/Neon marketplace integration, not part of this project.
.agents/
skills-lock.json
.claude/skills/neon
.claude/skills/neon-postgres
