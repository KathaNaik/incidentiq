"""Ingestion for `VladislavMarinovich/polaris-support-tickets-v2` (CC BY-SA 4.0).

Polaris is an external evaluation benchmark, not application content. Its ground-truth
labels are split into a separate module, separate model, and separate output file from
the observable features, so runtime code cannot reach a label by accident — there is no
object in the feature path that holds one.
"""
