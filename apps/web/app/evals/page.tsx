import { Placeholder } from "@/components/placeholder";

export default function EvalsPage() {
  return (
    <Placeholder
      title="Evals"
      description="Evaluation runs and metrics: triage accuracy, correlation precision and recall, retrieval Recall@K, unsupported recommendation rate, and abstention behaviour."
    />
  );
}
